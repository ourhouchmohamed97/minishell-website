{
  "name": "minishell-backend",
  "version": "1.0.0",
  "description": "WebSocket server for minishell",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "make || echo 'No makefile, skipping build'",
    "postinstall": "npm rebuild node-pty --build-from-source && npm run build"
  },
  "dependencies": {
    "node-pty": "^1.0.0",
    "ws": "^8.14.2"
  }
}